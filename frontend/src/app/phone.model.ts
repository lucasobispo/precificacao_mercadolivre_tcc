export class Phone {
  // memory: number;
  // ram_memory: number;
  // Marca: string;
	// Modelo: string;
  // condition: string;

  memoria_interna: number;
  memoria_ram: number;
  quantidade_sims: number;
  ano_lancamento: number;
  marca_renomeado_upper: string;
  condition: string;
  Linha: string;
  Marca: string;
}
